import { Routes } from '@angular/router';
import { ContributionComponent } from './features/contribution/contribution.component';

export const routes: Routes = [{ path: '', component: ContributionComponent }];
